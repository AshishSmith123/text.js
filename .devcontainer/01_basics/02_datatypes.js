"use strict"; // treat all js code as new version

// alert(3+3) // we are using nodejs, not browser

console.log(3    
    +3) // Do not do this , code readiability should be high 
    console.log("Ashish")

    let name = "Ashish"
    let age = 17
    let isLoggedin = false
    let state = null;

    // if number => 2^53
    // we'll use => bigint
    //string => ""
    //boolean => true/false
    //null => stand alnoe value
    //undefined => representation of empty value
    //symbol => unique

    //object

    console.log(typeof undefined)